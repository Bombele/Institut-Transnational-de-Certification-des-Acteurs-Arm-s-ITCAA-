# .github/workflows/offline-ai.yml

name: Offline AI Validation

on:
  push:
    branches:
      - feature/ai-offline-module
  pull_request:
    branches:
      - main

jobs:
  test-offline-ai:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Installer les dépendances IA
        run: |
          python -m venv venv
          source
