name: Vérification de la structure ITCAA

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-structure:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: 📦 Installer les dépendances
        run: pip install -r requirements.txt

      - name: 🧪 Vérifier la structure du projet
        run: python check_structure.py

      - name: 🧠 Valider la configuration Render
        run: python validate_render_config.py
