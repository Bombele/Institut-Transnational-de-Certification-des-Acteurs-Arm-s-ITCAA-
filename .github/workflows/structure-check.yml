name: Structure & Tests Check

on:
  push:
    branches:
      - main
      - feature/ai-offline-module
  pull_request:
    branches:
      - main
      - feature/ai-offline-module

jobs:
  validate-structure-and-tests:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: 🐍 Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Vérifier requirements.txt
        run: |
          if [ ! -f requirements.txt ]; then
            echo "❌ requirements.txt manquant à la racine"
            exit 1
          else
            echo "✅ requirements.txt trouvé"
          fi

      - name: 📂 Vérifier module IA
        run: |
          if [ ! -f src/itcaa_ai_offline/__init__.py ]; then
            echo "❌ __init__.py manquant dans src/itcaa_ai_offline/"
            exit 1
          else
            echo "✅ Module itcaa_ai_offline correctement structuré"
          fi

      - name: 🧪 Vérifier dossier tests
        run: |
          if [ ! -d tests ]; then
            echo "❌ Dossier tests/ manquant"
            exit 1
          else
            echo "✅ Dossier tests/ trouvé"
          fi

      - name: 📦 Installer les dépendances
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov black isort mypy

      - name: 🎯 Lint avec Black, Isort et Mypy
        run: |
          black --check src/ tests/
          isort --check-only src/ tests/
          mypy   # utilise mypy.ini à la racine

      - name: 🧠 Configurer le PYTHONPATH
        run: echo "PYTHONPATH=$(pwd):$(pwd)/src" >> $GITHUB_ENV

      - name: 🧪 Lancer les tests avec couverture
        run: |
          pytest tests/ --cov=itcaa_ai_offline --cov-report=xml --cov-report=term-missing

      - name: 📊 Vérifier le rapport de couverture
        run: |
          if [ ! -f coverage.xml ]; then
            echo "❌ Rapport de couverture introuvable"
            exit 1
          else
            echo "✅ Rapport de couverture généré"
          fi

      - name: 🗂️ Archiver le rapport de couverture
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
- name: 🎯 Lint avec Black, Isort et Mypy
  run: |
    black --check src/ tests/ || (echo "❌ Black a trouvé des erreurs" && exit 1)
    isort --check-only src/ tests/ || (echo "❌ Isort a trouvé des erreurs" && exit 1)
    mypy || (echo "❌ Mypy a trouvé des erreurs" && exit 1)
